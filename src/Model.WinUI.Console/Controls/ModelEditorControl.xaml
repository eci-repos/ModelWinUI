<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="ModelConsole.Controls.ModelEditorControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ModelConsole.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

   <Grid>
      <Grid.RowDefinitions>
         <RowDefinition Height="*" />
      </Grid.RowDefinitions>
      <Grid.ColumnDefinitions>
         <ColumnDefinition Width="*" />
         <ColumnDefinition Width="Auto" />
         <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>

      <!-- Right panel: log on top, entity inspector below. Declared first
           so its view model subscribes to the log before ModelPanelControl
           writes its startup message (backlog 014). -->
      <Grid x:Name="RightPanel" Grid.Column="2" Grid.Row="0" MinWidth="250">
         <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
         </Grid.RowDefinitions>
         <local:DiagnosticsLogControl x:Name="DiagnosticsPanel" Margin="0"
            Grid.Column="0" Grid.Row="0" VerticalAlignment="Stretch"/>
         <local:EntityInspectorControl x:Name="InspectorPanel" Margin="0"
            Grid.Column="0" Grid.Row="1" VerticalAlignment="Stretch"/>
      </Grid>

      <!-- Toggle strip: collapses/expands the right panel so the drawing
           reflows into the freed space. It lives in its own column so the
           button stays reachable while the panel is collapsed. -->
      <Border Grid.Column="1" Grid.Row="0"
              Background="#fbfbfb"
              BorderBrush="LightGray"
              BorderThickness="1,0,0,0">
         <Button x:Name="RightPanelToggle" Click="ToggleRightPanel_Click"
                 VerticalAlignment="Center"
                 Background="Transparent" BorderThickness="0"
                 Padding="4,8"
                 ToolTipService.ToolTip="Collapse right panel">
            <FontIcon x:Name="ToggleGlyph" Glyph="&#xE76B;" FontSize="12" />
         </Button>
      </Border>

      <local:ModelPanelControl x:Name="ModelPanel" Grid.Column="0" />
   </Grid>
</UserControl>
