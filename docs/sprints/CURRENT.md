# Current Sprint

**No sprint is currently in execution.**

The last sprint (2026-08-18, proof sample model — backlog item `020`) was promoted to `docs/sprints/archive/sprint-2026-08-18-proof-sample-model.md`. The `020` gate **passed** (R7 declared-beats-inferred resolved the ambiguous-name case; the grouped sample loads through `019` with no renderer/explorer changes).

The next sprint is seeded from backlog item `021` (type + enumeration wiring — surface the canonical type and enumeration on the renderers). When it is defined, copy `docs/sprints/_TEMPLATE.md` here and start executing.
