# Current Sprint

**No sprint is currently in execution.**

The last sprint (2026-08-19, pointer-hover metadata readout — backlog item `027`) was promoted to `docs/sprints/archive/sprint-2026-08-19-pointer-hover-metadata-readout.md`. `027` is part 1 of the **object-oriented drawing series**: hovering a table or FK connector on the XAML canvas shows that object's metadata (description, column count, PK/FK counts, provenance for tables; cardinality and roles for connectors) — a delay-triggered, pointer-following, strictly read-only tooltip sourced from the live model objects via the portable `HoverSummary` provider.

**The object-oriented drawing series is in progress: `027` is done, `028` (graph node objects — every drawable is a typed node: identity, live model ref, hover summary, edit verbs) and `029` (editable entities — rename/add/remove entities + columns, edit FK targets/cardinality/roles/metadata from the inspector) remain.** When the next sprint is defined, copy `docs/sprints/_TEMPLATE.md` here and start executing.
