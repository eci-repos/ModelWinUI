# Current Sprint

**No sprint is currently in execution.**

The last sprint (2026-08-18, JSON Schemas for model representations — backlog item `025`) was promoted to `docs/sprints/archive/sprint-2026-08-18-json-schemas-for-model-representations.md`. `025` shipped a JSON Schema per representation (`Schemas/array.schema.json` + `Schemas/grouped.schema.json`) and validated any input model against it at load time (warn channel, never a hard block).

**The node-enrichment series (023 containerized structure, 024 descriptions, 026 per-node provenance, 025 JSON Schemas) is complete and the backlog is empty.** v2 (generalization, uniqueness, referential-integrity, stereotypes) remains designed-for, not built — a candidate next series. When the next sprint is defined, copy `docs/sprints/_TEMPLATE.md` here and start executing.
